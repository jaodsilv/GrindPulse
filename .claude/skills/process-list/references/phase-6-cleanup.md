# Phase 6: Cleanup

This phase has no queue. Only 1 script run is needed.

```
Bash(python .claude/scripts/cleanup.py)
```

Print the returned summary to the user. The pipeline is complete.

CRITICAL: This phase does not follow the dispatch protocol, only 1 agent of this type should be spawned; No more agents are needed.
